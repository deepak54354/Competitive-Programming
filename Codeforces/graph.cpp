#include <bits/stdc++.h>
using namespace std;

typedef struct node{
	int val;
	struct node* next;
}

class stack{
	public:
		stack() // constuctor
		void push(int val) // push on the top of stack
		void pop() // pops top element of the stack
		int top() // returns top element
}

class graph{
	int V;
	
}